#!/usr/bin/env ruby
require_relative  "../lib/turn"
board = Array.new(9," ")
puts("Welcome to Tic Tac Toe!")
display_board(board)
turns = 1
until turns == 9
  turn(board)
  turns+=1
  display_board(board)
end